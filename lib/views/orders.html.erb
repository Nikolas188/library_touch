<!DOCTYPE html>
<html>
  <head>
    <title>authors</title>
  </head>
  <body>
    <h1>orders</h1>
    <% @library.orders.each do |order| %>
    <%= "#{order.book.author.name} - #{order.book.title}. #{order.date}. #{order.reader.email}" %>
    <br>
    <% end %>
    <form id="create-book" method="post">
      <label for="book">Book:</label>
      <select name="book" form="create-book">
      <% @library.books.uniq(&:title).each do |book| %>
      <option value="<%= book.title %>">
       <%= book.title %>
      </option>
      <% end %>
      </select>
      <br>
      <br>
      <label for="reader">Reader:</label>
      <select name="reader" form="create-reader">
      <% @library.readers.uniq(&:email).each do |reader| %>
      <option value="<%= reader.email %>">
      <%= reader.email %>
      </option>
      <% end %>
      </select>
      <br>
      <br>
      <label for="date">Date:</label>
      <label for="date">Date:</label>
      <select name="date" form="create-order">
        <option value="<%= Date.today %>">
          <%= Date.today %>
        </option>
      <input type="submit" value="Save">
    </form>
    <a href="/">BACK TO MAIN</a>
  </body>
</html>
